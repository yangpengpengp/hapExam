<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.hand.hap.hapExam.mapper.ArCustomersMapper">
    <resultMap id="BaseResultMap" type="com.hand.hap.hapExam.dto.ArCustomers">
        <result column="CUSTOMER_ID" property="customerId" jdbcType="DECIMAL" />
        <result column="CUSTOMER_NUMBER" property="customerNumber" jdbcType="VARCHAR" />
        <result column="CUSTOMER_NAME" property="customerName" jdbcType="VARCHAR" />
        <result column="COMPANY_ID" property="companyId" jdbcType="DECIMAL" />
        <result column="ENABLED_FLAG" property="enabledFlag" jdbcType="VARCHAR" />
    </resultMap>



    <select id="selectLovCustomers" resultMap="BaseResultMap">
        SELECT
            CUSTOMER_ID,
            CUSTOMER_NUMBER,
            CUSTOMER_NAME,
            COMPANY_ID,
            ENABLED_FLAG
        FROM
            hap_ar_customers
        <where>
            <if test="customerId!=null">
                AND CUSTOMER_ID=#{customerId}
            </if>
            <if test="customerName!=null">
                AND customer_name like concat ('%', concat (#{customerName}, '%'))
            </if>
            <if test="companyId!=null">
                AND company_id=#{companyId}
            </if>
        </where>
    </select>

</mapper>